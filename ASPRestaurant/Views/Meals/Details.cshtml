@model ASPRestaurant.Data.Meal

@{
	ViewData["Title"] = "Детайли";
}

<div class="details-wrapper">

	<div class="details-card">

		<div class="details-image">
			<img src="@(Model.CoverImage ?? "https://via.placeholder.com/600x400")"
				 alt="@Model.Name" />
		</div>

		<div class="details-body">

			<h1 class="details-title">@Model.Name</h1>

				<p class="details-desc">
					⚠️ Алергени:
					<strong>
						@(string.IsNullOrWhiteSpace(Model.Alergens)
												? "няма"
												: Model.Alergens)
					</strong>
			</div>

		<div class="details-info">

			<div class="info-box">
				🍽 Категория: <strong>@Model.TypeOrders?.Name</strong>
			</div>

			<div class="info-box">
				⚖️ Грамаж: <strong>@Model.Grammage g</strong>
			</div>

			<div class="info-box">
				💰 Цена: <strong>@(Model.Price?.ToString("0.00") ?? "0.00") €</strong>
			</div>


			<div class="details-actions">

				<a href="javascript:history.back()" class="btn-back">
					← Назад
				</a>

			</div>
		</div>

	</div>
</div>

